* xf86-video-modesetting (not a kernel issue)
* usb warnings
* otg
* libertas_sdio:
	suspend/resume:
		1. resume unclean catched by MMC new card detected
		2. In xfce-image suspend by power button: PM: Device mmc1:0001 failed to suspend: error -38
	power off: 
		fails with -38
* omap_fb:
	1. recipe to enable: echo 'install omapdrm /sbin/modprobe omapfb' > /etc/modprobe.d/omapfb.conf
	2. works only on very cold boot (power up)

